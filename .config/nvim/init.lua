-- bootstrap lazy.nvim, LazyVim and your plugins

require("config.lazy")

-- FIX: git is not tracking the dotfile repo properly
-- may be something to do with the .config ,perhaps it is not considered as a folder
